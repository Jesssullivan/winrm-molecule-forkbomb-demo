---
dependency:
  name: galaxy

driver:
  name: default

platforms:
  - name: vmnode852
    managed: false
    groups:
      - windows

provisioner:
  name: ansible
  inventory:
    links:
      hosts: ../../../../../../molecule/inventory.yml
  config_options:
    defaults:
      serial: 1

verifier:
  name: ansible
